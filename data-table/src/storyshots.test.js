import initStoryshots from "@storybook/addon-storyshots";
import "jest-styled-components"; //helps to avoid classname mismatch for styled components snapshots

initStoryshots({
  /* configuration options */
});
